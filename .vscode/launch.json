{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debug Server",
      "request": "launch",
      "command": "npm run dev:server",
      "cwd": "${workspaceFolder}/backend"
    }
  ]
}
